import { Typography } from '@mui/material';

const ProjectPage = ({ userId }) => {
    return (
        <div>
        <Typography variant="h4" gutterBottom>
            To-Do: Page Not Implemented
        </Typography>
        </div>
    );
};

export default ProjectPage;
